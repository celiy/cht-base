import type { App } from "vue";
import type { Router } from "vue-router";

export function setupAuthGuard(_router: Router): void {
    void _router;
}

export async function installClientPlugins(_app: App, _router: Router): Promise<void> {
    void _app;
    void _router;
}
